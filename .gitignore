node_modules/
.vscode/
dist/
# Coverage reports from test runs (JEST)
coverage/
# Real data from the raw API should not be committed (privacy reasons). Used for creating test cases with mock data.
tests/samples/ 
*.log
.DS_Store